//////////////////////////////////////////////////////////////////////////////////////////////
// DO NOT MODIFY THIS FILE                                                                  //
// This file is automatically generated by ZenStack CLI and should not be manually updated. //
//////////////////////////////////////////////////////////////////////////////////////////////

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum Role {
  ADMIN
  USER
}

enum TitleType {
  Movie
  Show
}

enum MainProfession {
  ACTOR
  DIRECTOR
  WRITER
  PRODUCER
  COMPOSER
  EDITOR
  CINEMATOGRAPHER
  COSTUME_DESIGNER
  PRODUCTION_DESIGNER
  ART_DIRECTOR
  MAKEUP_ARTIST
  SOUND
  VISUAL_EFFECTS
  SPECIAL_EFFECTS
  CASTING
}

enum ProductionType {
  Title
  Episode
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model Company {
  id            String   @id() @default(uuid())
  createdAt     DateTime @default(now()) @map("created_at")
  updatedAt     DateTime @updatedAt() @map("updated_at")
  name          String
  logoPath      String?  @map("logo_path")
  originCountry String   @map("origin_country")
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model Episode {
  id                      String     @id() @default(uuid())
  number                  Int
  duration                Int
  stillPath               String?    @map("still_path")
  season                  Season     @relation(fields: [seasonId], references: [id], onDelete: Cascade)
  seasonId                String     @map("season_id")
  delegate_aux_production Production @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: Cascade)
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
/// @@delegate(productionType)
model Production {
  id                   String                  @id() @default(uuid())
  createdAt            DateTime                @default(now()) @map("created_at")
  updatedAt            DateTime                @updatedAt() @map("updated_at")
  tmdbId               String                  @unique() @map("tmdb_id")
  originalName         String                  @map("original_name")
  originalLanguage     String                  @map("original_language")
  releaseDate          DateTime?               @map("release_date")
  voteAverage          Float?                  @map("vote_average")
  voteCount            Int?                    @map("vote_count")
  translations         ProductionTranslation[]
  cast                 ProductionTeam?         @relation("Cast")
  crew                 ProductionTeam?         @relation("Crew")
  productionType       ProductionType
  delegate_aux_episode Episode?
  delegate_aux_title   Title?
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model ProductionTranslation {
  id           String     @id() @default(uuid())
  createdAt    DateTime   @default(now()) @map("created_at")
  updatedAt    DateTime   @updatedAt() @map("updated_at")
  name         String
  overview     String?
  posterPath   String?    @map("poster_path")
  language     Language   @relation(fields: [languageCode], references: [code], onDelete: Cascade)
  languageCode String     @map("language_code")
  production   Production @relation(fields: [productionId], references: [id], onDelete: Cascade)
  productionId String     @map("production_id")
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model Language {
  id                     String                  @id() @default(uuid())
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt() @map("updated_at")
  code                   String                  @unique()
  name                   String?
  productionTranslations ProductionTranslation[]
  seasonTranslations     SeasonTranslation[]
  genreTranslations      GenreTranslation[]
  subGenreTranslations   SubGenreTranslation[]
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model SeasonTranslation {
  id           String   @id() @default(uuid())
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt() @map("updated_at")
  overview     String?
  language     Language @relation(fields: [languageCode], references: [code], onDelete: Cascade)
  languageCode String   @map("language_code")
  season       Season   @relation(fields: [seasonId], references: [id], onDelete: Cascade)
  seasonId     String   @map("season_id")

  @@unique([seasonId, languageCode])
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model Season {
  id           String              @id() @default(uuid())
  createdAt    DateTime            @default(now()) @map("created_at")
  updatedAt    DateTime            @updatedAt() @map("updated_at")
  number       Int
  posterPath   String?             @map("poster_path")
  backdropPath String?             @map("backdrop_path")
  show         Show                @relation(fields: [showId], references: [id], onDelete: Cascade)
  showId       String              @map("show_id")
  episodes     Episode[]
  translations SeasonTranslation[]
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model Show {
  id                 String   @id() @default(uuid())
  seasons            Season[]
  delegate_aux_title Title    @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: Cascade)
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
/// @@delegate(titleType)
model Title {
  id                      String          @id() @default(uuid())
  backdropPath            String?         @map("backdrop_path")
  popularity              Float?
  adult                   Boolean
  genres                  TitleGenre[]
  subGenres               TitleSubGenre[]
  titleType               TitleType
  delegate_aux_show       Show?
  delegate_aux_movie      Movie?
  delegate_aux_production Production      @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: Cascade)
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model TitleGenre {
  id        String   @id() @default(uuid())
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt() @map("updated_at")
  title     Title    @relation(fields: [titleId], references: [id], onDelete: Cascade)
  titleId   String   @map("title_id")
  genre     Genre    @relation(fields: [genreId], references: [id], onDelete: Cascade)
  genreId   String   @map("genre_id")

  @@unique([titleId, genreId])
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model Genre {
  id                String             @id() @default(uuid())
  createdAt         DateTime           @default(now()) @map("created_at")
  updatedAt         DateTime           @updatedAt() @map("updated_at")
  tmdbId            Int?               @unique() @map("tmdb_id")
  subGenres         SubGenre[]         @relation("SubGenreGenre")
  titles            TitleGenre[]
  genreTranslations GenreTranslation[]
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model SubGenre {
  id           String                @id() @default(uuid())
  createdAt    DateTime              @default(now()) @map("created_at")
  updatedAt    DateTime              @updatedAt() @map("updated_at")
  genres       Genre[]               @relation("SubGenreGenre")
  titles       TitleSubGenre[]
  translations SubGenreTranslation[]
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model TitleSubGenre {
  id         String   @id() @default(uuid())
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt() @map("updated_at")
  title      Title    @relation(fields: [titleId], references: [id], onDelete: Cascade)
  titleId    String   @map("title_id")
  subGenre   SubGenre @relation(fields: [subGenreId], references: [id], onDelete: Cascade)
  subGenreId String   @map("subGenre_id")

  @@unique([titleId, subGenreId])
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model SubGenreTranslation {
  id           String   @id() @default(uuid())
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt() @map("updated_at")
  name         String
  languageCode String   @map("language_code")
  language     Language @relation(fields: [languageCode], references: [code], onDelete: Cascade)
  subGenreId   String   @map("sub_genre_id")
  subGenre     SubGenre @relation(fields: [subGenreId], references: [id], onDelete: Cascade)

  @@unique([subGenreId, languageCode])
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model GenreTranslation {
  id           String   @id() @default(uuid())
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt() @map("updated_at")
  name         String
  languageCode String   @map("language_code")
  language     Language @relation(fields: [languageCode], references: [code], onDelete: Cascade)
  genreId      String   @map("genre_id")
  genre        Genre    @relation(fields: [genreId], references: [id], onDelete: Cascade)

  @@unique([genreId, languageCode])
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model ProductionTeam {
  id               String             @id() @default(uuid())
  createdAt        DateTime           @default(now()) @map("created_at")
  updatedAt        DateTime           @updatedAt() @map("updated_at")
  castProductionId String             @unique() @map("cast_production_id")
  castProduction   Production         @relation("Cast", fields: [castProductionId], references: [id], onDelete: Cascade)
  crewProductionId String             @unique() @map("crew_production_id")
  crewProduction   Production         @relation("Crew", fields: [crewProductionId], references: [id], onDelete: Cascade)
  members          ProductionMember[]
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model ProductionMember {
  id               String         @id() @default(uuid())
  createdAt        DateTime       @default(now()) @map("created_at")
  updatedAt        DateTime       @updatedAt() @map("updated_at")
  role             String
  person           Person         @relation(fields: [personId], references: [id], onDelete: Cascade)
  personId         String         @map("person_id")
  productionTeam   ProductionTeam @relation(fields: [productionTeamId], references: [id], onDelete: Cascade)
  productionTeamId String         @map("production_team_id")

  @@unique([productionTeamId, personId])
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model Person {
  id                 String             @id() @default(uuid())
  createdAt          DateTime           @default(now()) @map("created_at")
  updatedAt          DateTime           @updatedAt() @map("updated_at")
  name               String
  mainProfession     MainProfession?    @map("main_profession")
  birthday           DateTime?
  deathday           DateTime?
  bio                String?
  profilePicturePath String?            @map("profile_picture_path")
  productionMembers  ProductionMember[]
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
model Movie {
  id                 String @id() @default(uuid())
  duration           Int?
  delegate_aux_title Title  @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: Cascade)
}

/// @@allow('create,update', auth().role == ADMIN)
/// @@allow('read', true)
/// @@allow('create,read', true)
/// @@allow('all', auth().id == this.id)
model User {
  id                 String   @id() @default(uuid())
  createdAt          DateTime @default(now()) @map("created_at")
  updatedAt          DateTime @updatedAt() @map("updated_at")
  /// @email
  email              String   @unique()
  username           String   @unique()
  firstName          String   @map("first_name")
  lastName           String   @map("last_name")
  birthday           DateTime
  bio                String?
  /// @omit
  /// @password
  password           String
  profilePicturePath String?  @map("profile_picture_path")
  /// @allow('update', auth().role == ADMIN)
  role               Role     @default(USER)
}
