import "production"
import "season"

model Episode extends Production {
  number        Int
  duration      Int
  stillPath     String?               @map("still_path")
  season        Season                @relation(fields: [seasonId], references: [id], onDelete: Cascade)
  seasonId      String                @map("season_id")
}